---
import type { Step } from '../../types/landing';

interface Props {
  step: Step;
}

const { step } = Astro.props;

// Icon mapping
const iconMap: Record<string, string> = {
  'user-plus': '👤',
  'map-pin': '📍',
  'bell': '🔔',
  'heart': '❤️',
};

const iconDisplay = step.icon ? (iconMap[step.icon] || '→') : '→';
---

<div class="relative flex flex-col items-center text-center space-y-4 p-6">
  <!-- Step Number Badge -->
  <div class="relative">
    <div class="flex items-center justify-center w-20 h-20 rounded-full bg-gradient-to-br from-primary-500 to-primary-700 text-white text-3xl font-bold shadow-strong">
      {step.number}
    </div>

    <!-- Icon Badge (overlapping) -->
    {step.icon && (
      <div class="absolute -bottom-2 -right-2 flex items-center justify-center w-10 h-10 rounded-full bg-white border-2 border-primary-500 text-xl shadow-md">
        {iconDisplay}
      </div>
    )}
  </div>

  <!-- Title -->
  <h3 class="text-xl font-bold text-secondary-900 mt-2">
    {step.title}
  </h3>

  <!-- Description -->
  <p class="text-secondary-600 text-base leading-relaxed max-w-xs">
    {step.description}
  </p>
</div>
