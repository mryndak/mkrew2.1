---
import Layout from '@/layouts/Layout.astro';
import { RegisterFormWrapper } from '@/components/auth/RegisterFormWrapper';

/**
 * Register Page
 * Static page with React islands for interactivity
 *
 * Features:
 * - Static generation for fast loading
 * - React island dla RegisterForm (client:only)
 * - Redux Provider dla zarządzania stanem (dla potencjalnych przyszłych funkcji)
 * - Multi-step registration form (3 kroki)
 * - Draft persistence (sessionStorage)
 * - Email uniqueness check (async)
 * - Password strength validation
 * - Redirect to /verify-email-pending po sukcesie
 */
---

<Layout
  title="Zarejestruj się | mkrew"
  description="Utwórz konto w platformie mkrew i dołącz do społeczności dawców krwi w Polsce."
  includeNavbar={true}
  includeFooter={true}
>
  <Fragment slot="head">
    <meta name="robots" content="noindex, nofollow" />
  </Fragment>

  <main class="flex-1 flex items-center justify-center bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
    <div class="w-full max-w-4xl">
      {/* Header */}
      <div class="text-center mb-8">
        <h1 class="text-3xl font-bold text-gray-900 mb-2">
          Dołącz do mkrew
        </h1>
        <p class="text-sm text-gray-600">
          Utwórz bezpłatne konto i zacznij pomagać
        </p>
      </div>

      {/* Register Form - React island with Redux */}
      <RegisterFormWrapper client:load />

      {/* Links section */}
      <div class="mt-8 text-center">
        {/* Login link */}
        <div class="text-sm text-gray-600">
          Masz już konto?{' '}
          <a
            href="/login"
            class="text-blue-600 hover:text-blue-700 hover:underline font-semibold"
          >
            Zaloguj się
          </a>
        </div>
      </div>

      {/* Security & Privacy notice */}
      <div class="mt-6 max-w-2xl mx-auto">
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
          <div class="flex">
            <div class="flex-shrink-0">
              <svg
                class="h-5 w-5 text-blue-400"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
                aria-hidden="true"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                />
              </svg>
            </div>
            <div class="ml-3 flex-1">
              <p class="text-xs text-blue-800">
                <strong>Bezpieczeństwo:</strong> Twoje hasło jest bezpiecznie szyfrowane.
                Dane formularza są zapisywane lokalnie w przeglądarce (bez hasła) i możesz
                wrócić do rejestracji w dowolnym momencie.
              </p>
            </div>
          </div>
        </div>
      </div>

      {/* Help section */}
      <div class="mt-6 text-center">
        <p class="text-xs text-gray-500">
          Masz problem z rejestracją?{' '}
          <a href="/kontakt" class="underline hover:text-gray-700">
            Skontaktuj się z nami
          </a>
        </p>
      </div>
    </div>
  </main>
</Layout>
